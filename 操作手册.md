# WeChatTweak 备份与重装操作手册

## 1. 当前适用环境

- 电脑：Intel Mac
- 微信版本：4.1.12
- 微信构建号：269341
- 应用位置：`/Applications/WeChat.app`
- 功能：拦截消息撤回，并在发送者头像下方显示「[已撤回]」

Intel Mac 会直接运行微信的 `x86_64` 版本，不需要安装 Rosetta 2，也不需要设置“使用 Rosetta 打开”。

> 本补丁只适用于微信 4.1.12（269341）。不要使用当前配置修改其他微信版本。

## 2. 重装系统前备份

### 2.1 退出微信

先在微信菜单中选择“退出微信”，确保微信完全退出后再备份。

### 2.2 备份已经修改好的微信

建议把整个 `WeChat.app` 压缩成一个文件，避免外置硬盘文件系统丢失应用包的属性。

把下面命令中的 `/Volumes/备份盘` 改成实际的外置硬盘路径：

```bash
ditto -c -k --sequesterRsrc --keepParent \
  /Applications/WeChat.app \
  "/Volumes/备份盘/WeChat-4.1.12-269341-patched.zip"
```

备份完成后，确认外置硬盘中存在：

```text
WeChat-4.1.12-269341-patched.zip
```

不要只复制微信主程序、配置文件或者单独的动态库，必须保留完整的 `WeChat.app`。

### 2.3 备份补丁项目

建议同时把整个 `WeChatTweak` 项目目录复制到外置硬盘。当前版本包含本地定制代码，普通 Homebrew 版本不一定包含图片、文件、视频和链接等消息类型的撤回标记修改。

如果不备份整个项目，至少保留以下三个文件：

```text
wechattweak
config.json
libWeChatTweakMenu.dylib
```

这三个文件必须放在同一个目录中。

### 2.4 备份聊天记录（可选）

复制 `WeChat.app` 只会备份微信程序和补丁，不会包含聊天记录。

聊天记录建议使用 macOS 的“迁移助理”或“时间机器”迁移。手动备份时，先退出微信，再备份：

```text
~/Library/Containers/com.tencent.xinWeChat
```

## 3. 重装系统后恢复

### 3.1 恢复微信应用

如果系统中已经安装了其他版本的微信，先把它退出并移到废纸篓。然后执行：

```bash
sudo ditto -x -k \
  "/Volumes/备份盘/WeChat-4.1.12-269341-patched.zip" \
  /Applications
```

恢复后应该存在：

```text
/Applications/WeChat.app
```

Intel Mac 不需要安装 Rosetta 2，可以直接启动微信。

### 3.2 首次启动

在“应用程序”中打开微信，登录后检查以下功能：

1. 微信能够正常启动，没有闪退。
2. 对方撤回文字消息后，原消息仍然保留。
3. 对方撤回图片、文件、视频、视频号或链接后，原消息仍然保留。
4. 被撤回消息的发送者头像下方显示「[已撤回]」。
5. 上下滚动聊天列表时，头像和「[已撤回]」之间的间距保持稳定。

## 4. 系统提示无法打开或应用已损坏

先移除从外置硬盘恢复时可能产生的隔离属性：

```bash
sudo xattr -dr com.apple.quarantine /Applications/WeChat.app
```

然后重新打开微信。

如果仍然无法打开，检查应用签名：

```bash
codesign --verify --deep --strict --verbose=2 /Applications/WeChat.app
```

正常结果应包含：

```text
/Applications/WeChat.app: valid on disk
/Applications/WeChat.app: satisfies its Designated Requirement
```

## 5. 使用补丁文件重新安装

只有在完整的 `WeChat.app` 备份无法使用时，才采用本方法。

首先准备未经修改的微信 4.1.12（269341），放到：

```text
/Applications/WeChat.app
```

退出微信，进入保存补丁文件的目录，然后执行：

```bash
./wechattweak patch \
  --app /Applications/WeChat.app \
  --config ./config.json \
  --menu-dylib ./libWeChatTweakMenu.dylib
```

执行完成后再启动微信。

> 如果微信不是 4.1.12（269341），请停止操作。当前 `config.json` 中的地址不能用于其他构建号。

## 6. 后续使用注意事项

- 不要在微信内部执行自动升级。
- 微信被升级或重新安装后，补丁通常会被覆盖。
- 不要用新版微信直接覆盖当前的 `WeChat.app`。
- 建议长期保留完整的压缩备份和 `WeChatTweak` 项目备份。
- 恢复后如果“拦截撤回”有效但没有「[已撤回]」标记，先确认运行的是 Intel `x86_64` 版本，并确认安装的是本手册对应的自定义动态库。
- 出现连续闪退时，先停止反复启动，保存最新的崩溃报告后再排查。

## 7. 快速恢复清单

```text
1. 重装 macOS
2. 不需要安装 Rosetta 2
3. 解压 WeChat-4.1.12-269341-patched.zip 到 /Applications
4. 如被系统拦截，执行 xattr 命令
5. 启动微信并检查撤回拦截和头像标记
6. 使用迁移助理或时间机器恢复聊天记录
```
